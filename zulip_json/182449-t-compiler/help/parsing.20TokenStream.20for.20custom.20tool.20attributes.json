[
    {
        "content": "<p>Hi, I'm looking into parsing arguments to custom tool attributes, e.g.</p>\n<div class=\"codehilite\"><pre><span></span><code>#![feature(register_tool)]\n#![register_tool(mytool)]\n\n// I want to parse the arguments to my tool attributes\n#[mytool::foo(abc @ def, x @ y)]\nfn bar() {\n\n}\n</code></pre></div>\n<p>Hooking into rustc, I get  a <code>rustc_ast::tokenstream::TokenStream</code> for the arguments to <code>mytool::foo</code>, which I'd like to parse. Currently I'm using a custom ad-hoc parser to parse really simple arguments, but that does not really scale to more complex attributes.<br>\nIs there some good existing approach to creating custom parsers for these TokenStreams?</p>\n<p>(I was hoping for something along the lines of the <code>parse</code> module provided by the <code>syn</code> crate, which however works on proc_macro TokenStreams)</p>",
        "id": 257499074,
        "sender_full_name": "Lennard Gäher",
        "timestamp": 1634200447
    }
]